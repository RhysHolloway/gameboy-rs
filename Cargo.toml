[workspace]
resolver = "3"
members = ["gameboy-core", "pixels", "web", "app", "desktop"]
default-members = ["gameboy-core", "pixels", "web", "app", "desktop"]

[workspace.package]
edition = "2024"

[profile.release]
strip = true
panic = "abort"
