# Gameboy Emulator

Gameboy emulator coded in Rust

Desktop app under /desktop, use `cargo run --package desktop` to run

To run a ROM, drop a file into the window

![Pokemon Red running](docs/game.png)

![The debugger for the emulator](docs/debug.png)